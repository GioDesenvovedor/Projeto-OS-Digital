<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OSDigital" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!--  <item name="colorPrimary">@color/marron_button</item>  -->
        <item name="android:windowBackground">@color/back</item>
        <!--  <item name="colorPrimary">@color/marron_button</item>-->
         <item name="colorPrimaryVariant">@color/back</item>
         <item name="colorOnPrimary">@color/white</item>
         <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="CustomTabLayout">  parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textSize">14sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>

    </style>

    <!-- Definir Status bar color personalizada na activity. -->
    <style name="AppTheme.ColoredStatusBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Defina a cor da barra de status -->
        <item name="android:statusBarColor">@color/azul_button</item>
    </style>

    <style name="Theme.OSDigital" parent="Base.Theme.OSDigital" />

</resources>